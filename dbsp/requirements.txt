# Dead Ball Success Predictor — Python dependencies
# Install with:  pip install -r requirements.txt
streamlit>=1.36   # needs Python 3.10 or newer
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
plotly>=5.20
joblib>=1.3

# Optional: only needed if you want to pull REAL StatsBomb Open Data
# (set PREFER_REAL=1 before running train_models.py). The app works fully
# without it using the built-in synthetic dataset.
statsbombpy>=1.11
